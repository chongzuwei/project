<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           https://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc
           https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 1. Scan controller package -->
    <context:component-scan base-package="com.secj3303.controller" />

    <!-- 2. Enable Spring MVC annotation support with multipart file upload (50MB max) -->
    <mvc:annotation-driven>
        <mvc:message-converters>
            <bean class="org.springframework.http.converter.StringHttpMessageConverter">
                <constructor-arg value="UTF-8"/>
            </bean>
        </mvc:message-converters>
    </mvc:annotation-driven>

    <!-- Multipart file upload resolver for handling file uploads (50MB max file, 100MB max request) -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="52428800"/> <!-- 50MB in bytes -->
        <property name="maxInMemorySize" value="1048576"/> <!-- 1MB in memory -->
    </bean>

    <!-- 3. JSP View Resolver (for .jsp files) -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/templates/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- 4. Message Source for i18n and message expressions -->
    <bean id="messageSource" class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
        <property name="basename" value="classpath:messages"/>
        <property name="defaultEncoding" value="UTF-8"/>
    </bean>

    <!-- 5. Configure static resource handling (CSS, JS, images, etc.) -->
    <mvc:resources mapping="/resources/**" location="/resources/" />
    <mvc:resources mapping="/css/**" location="/css/" />
    <mvc:resources mapping="/js/**" location="/js/" />
    <mvc:resources mapping="/images/**" location="/images/" />
    <mvc:resources mapping="/*.css" location="/WEB-INF/templates/" />
    <mvc:resources mapping="/*.js" location="/WEB-INF/templates/" />
    <!-- Serve uploaded verification documents -->
    <mvc:resources mapping="/uploads/**" location="file:uploads/" />
    
    <!-- 6. Enable default servlet for static content -->
    <mvc:default-servlet-handler />

</beans>
